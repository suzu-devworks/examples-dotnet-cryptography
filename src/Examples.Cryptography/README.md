# Examples.Cryptography

## Overview

This library provides extensions and utilities for the .NET standard cryptography APIs (`System.Security.Cryptography`).
It simplifies common cryptographic operations and adds convenient helper methods for key management, certificate handling, and signing/verification.
